export default {
  // Location of the main dist folder
  distDir: '../../dist',
  // The folder within dist to use
  outputFolder: '/',
  // The prebuild index file
  currentIndexFileName: 'index.html',
  // File to make at each route
  outputFileName: 'index.html',
  // Read encoding
  encoding: 'utf8',
  routes: [
    '/',
    '/rsvp'
  ]
};
